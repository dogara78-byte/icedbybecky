# ICED By Becky — Add one cake pop (commit-ready)

Copy these two files over the repo root, preserving paths, then commit.
Built against the current live version of the page, so nothing else regresses.

## Files
- `cakepop-gallery.html` — adds the new pop to the grid; updates dropdown counts
  (Other 1->2, Moderate 2->3). Nothing else touched.
- `images/cakepops-coastal-blue.jpg` — new image, resized to 1600px wide (344 KB) to
  match site convention.

## Classification
Coastal blue cake pops — Occasion: Other, Tier: Moderate.

## Verified in Chromium
5 designs total; Moderate filter shows 3, Other shows 2, Other+Moderate shows 2.
No broken images, no JS errors.
